# comfy-ess

Custom ComfyUI nodes for ESS prompt tooling, including a template editor with syntax highlighting and prompt parsing.

## Features
- Prompt template editor with scope-aware highlighting
- Optional template parsing with deterministic seed
- Positive/negative prompt output support

## Install
Copy this folder into `ComfyUI/custom_nodes/` and restart ComfyUI.
